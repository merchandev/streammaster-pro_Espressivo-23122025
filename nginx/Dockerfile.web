FROM nginx:alpine

# Copiar configuración personalizada
COPY web.conf /etc/nginx/conf.d/default.conf

# Copiar archivos del frontend (Opcional, pero recomendado para producción)
# COPY ../frontend /usr/share/nginx/html
# Por ahora mantenemos el volumen del frontend para facilitar actualizaciones rápidas 
# sin reconstruir, pero la config de nginx es crítica horneada.

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
